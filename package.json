{
  "name": "jsv6-bcdc-server-side-demo",
  "version": "1.0.0",
  "private": true,
  "description": "PayPal JSv6 Checkout + BCDC server-side demo. Express proxy that keeps client_secret in .env, exposes /api/credentials for in-app editing, and masks sensitive fields in the request/response logs (first 5 chars + '*').",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev":   "node --watch server.js"
  },
  "dependencies": {
    "dotenv":  "^16.4.5",
    "express": "^4.19.2"
  }
}
